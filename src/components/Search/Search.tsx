import { Input } from "@/components/ui/input"

const SearchComponent = () => {
  return (
    <Input placeholder="Pesquise pelo nome ou categoria do produto" />
  );
};

export default SearchComponent;
